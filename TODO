- CouchRestを見る
  - specを真似する

- Arelと連携
- 常にinclude_docを止めて lazy load (_revsも)
- validate support (more schema)

- more spec
  - spec support (DBをdestroy => fixture? )
  - Book.find(:all) => {type: 'book'} Documentのclassで自動フィルタ
  - {type: 'blur'} から自動でクラス生成